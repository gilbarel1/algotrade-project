# n8n credentials

Placeholder — documented in Steps 3–10 (§6.4).

The OpenRouter credential is created once at the n8n level and reused by each agent
sub-workflow's own OpenRouter Chat Model node. On n8n < 1.78, use the OpenAI node with
base URL `https://openrouter.ai/api/v1`.
