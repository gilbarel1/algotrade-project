# Direct dependencies, pinned to the versions this project is tested against.
#
# Exact pins (==) rather than ranges: a report's figures depend on how pandas-ta computes
# an indicator and on how pypdf extracts text, so "it worked last month" has to remain
# reproducible for anyone cloning the repo. Transitive dependencies are deliberately left
# to pip's resolver — pinning those too would need a lockfile and a per-platform matrix
# (torch and playwright ship platform-specific wheels).
#
# Verified on Python 3.13 / Windows. Refresh with:
#   python -m pip list --format=freeze   (then update the lines below)

# --- Core service (Step 0) ---------------------------------------------------
fastapi==0.139.0
uvicorn[standard]==0.50.2
pydantic==2.13.4
duckdb==1.5.4
pyyaml==6.0.3
jinja2==3.1.6

# --- Data ingestion + indicators (Steps 1-2) ---------------------------------
yfinance==1.5.1
certifi==2026.6.17        # base CA bundle; augmented with the OS store for curl_cffi TLS
pandas==3.0.3
numpy==2.2.6
pandas-ta==0.4.71b0

# --- NLP sentiment (Step 4) --------------------------------------------------
transformers==5.13.0
torch==2.13.0             # large download; CPU inference is enough for FinBERT/DictaBERT

# --- Scraping / news (Steps 5-6) ---------------------------------------------
httpx==0.28.1
beautifulsoup4==4.15.0
feedparser==6.0.12
truststore==0.10.4        # OS-native TLS trust for httpx (corporate-proxy CAs); verify stays on
playwright==1.61.0        # Maya is a JS SPA behind bot protection; rendered headless (Step 6).
                          # One-time browser download: python -m playwright install chromium
pypdf==6.14.2             # Maya disclosure figures live in the report's PDF attachment, not on
                          # the report page (§3.2); text-extracted server-side. Read-only — the
                          # report PDF is still *written* by WeasyPrint below.

# --- PDF + charts (Step 9) ---------------------------------------------------
weasyprint==69.0
matplotlib==3.11.0

# --- Evaluation harness (Step 11) --------------------------------------------
tqdm==4.68.4              # progress bar for `python -m eval.run` (writes to stderr)

# --- Development: tests and linting (not needed to run the service) ----------
pytest==9.1.1             # python -m pytest   (the offline suite in tests/)
ruff==0.16.1              # python -m ruff check .
