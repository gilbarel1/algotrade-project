# Core service (required from Step 0)
fastapi
uvicorn[standard]
pydantic>=2
duckdb
pyyaml
jinja2

# Data ingestion + indicators (Steps 1-2)
yfinance
certifi           # base CA bundle; augmented with the OS store for curl_cffi TLS
pandas
numpy
pandas-ta

# NLP sentiment (Step 4)
transformers
torch

# Scraping / news (Steps 5-6)
httpx
beautifulsoup4
feedparser
truststore        # OS-native TLS trust for httpx (corporate-proxy CAs); verify stays on
playwright        # Maya is a JS SPA behind bot protection; rendered headless (Step 6).
                  # One-time browser download: python -m playwright install chromium

# PDF + charts (Step 9)
weasyprint
matplotlib
